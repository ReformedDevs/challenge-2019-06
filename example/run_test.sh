#!/bin/bash

# Put your name, the source (to be counted) and the command used run you solution
NAME=plusuncold
SOURCE=src/main.rs
RUN_COMMAND=./target/release/fact

# Uncomment this line
# echo "$USER, `"$RUN_COMMAND"`, `wc -c < $SOURCE`"
